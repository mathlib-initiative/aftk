name: Lean Action CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: leanprover/lean-action@v1
      - name: Process inventory tests
        run: lake env lean --run tests/process_inventory.lean
      - name: Dependency resolution tests
        run: ./tests/dependency.sh
      - name: Diagnostics daemon tests
        run: ./tests/server.sh
      - name: Technical-debt tests
        run: ./tests/tech_debt.sh
